<div class="min-h-screen bg-[#0f172a] text-gray-300 pt-32 pb-20 px-6">
    <div class="max-w-4xl mx-auto">
        
        <div class="mb-12 text-center">
            <span class="text-blue-400 font-bold tracking-widest uppercase text-xs mb-2 block">Cookies</span>
            <h1 class="text-3xl md:text-5xl font-serif font-bold text-white mb-4"><%= __('legal.cookies.title') %></h1>
        </div>

        <div class="bg-[#1e293b]/50 backdrop-blur-sm border border-gray-700 p-8 md:p-12 rounded-3xl shadow-2xl">
            <article class="prose prose-invert prose-lg max-w-none prose-headings:font-serif">
                
                <h3>1. <%= __('legal.cookies.sec1_title') %></h3>
                <p><%= __('legal.cookies.sec1_desc') %></p>

                <h3>2. <%= __('legal.cookies.sec2_title') %></h3>
                <div class="grid gap-4 not-prose my-6">
                    <div class="bg-gray-800 p-4 rounded-xl border border-gray-700">
                        <span class="text-green-400 font-bold text-xs uppercase"><%= __('legal.cookies.type1') %></span>
                        <p class="text-sm text-gray-300 mt-1"><%= __('legal.cookies.type1_desc') %></p>
                    </div>
                    <div class="bg-gray-800 p-4 rounded-xl border border-gray-700">
                        <span class="text-blue-400 font-bold text-xs uppercase"><%= __('legal.cookies.type2') %></span>
                        <p class="text-sm text-gray-300 mt-1"><%= __('legal.cookies.type2_desc') %></p>
                    </div>
                </div>

                <h3>3. <%= __('legal.cookies.sec3_title') %></h3>
                <p><%= __('legal.cookies.sec3_desc') %></p>

            </article>
        </div>
    </div>
</div>